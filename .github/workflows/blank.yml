name: Hello World
on: workflow_dispatch
jobs:
  Build:
    runs-on: ubuntu-latest
    steps:
      - name: Build Step
        run: echo "The build demo"

  Test:
    runs-on: ubuntu-latest
    needs: Build
    steps:
      - name: Test Step
        run: echo "Running Test cases"

  Deploy:
    runs-on: ubuntu-latest
    needs: Test
    steps:
      - name: Deploy step
        run: echo "This is deploy step"
